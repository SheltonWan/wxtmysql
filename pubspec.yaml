name: wxtmysql
description: "High-performance MySQL database service with connection pooling for Flutter/Dart applications. Supports concurrent operations, transaction management, and automatic connection lifecycle management."
version: 0.1.6
homepage:

environment:
  sdk: ^3.0.0

dependencies:
  mysql1: ^0.20.0
  logging: ^1.2.0
  dotenv: ^4.0.1
  synchronized: ^3.1.0+1

dev_dependencies:
  test: ^1.24.0
